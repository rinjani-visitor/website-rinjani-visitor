/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io', 'test.rinjanivisitor.com', 'i.ibb.co', 'firebasestorage.googleapis.com'],
  }
}

module.exports = nextConfig
